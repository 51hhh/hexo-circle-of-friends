# Vercel / Docker 用的 API 依赖。采集端用的是 hexo_circle_of_friends/requirements.txt，与本文件无关。
#
# 2026-09：这份钉版是 2022 年的，Vercel 的 Python 已经是 3.12、pip 已经是 25.x，于是两处同时失效：
#   1. uvicorn==0.14.0 的 wheel 元数据写了非法的 click (>=7.*)，pip >= 24.1 从"跳过并警告"改成硬失败
#   2. PyYAML 6.0 / lxml 4.6.4 / aiohttp 3.8.1 / SQLAlchemy 1.4.31 / pymongo 4.0.1 都没有 cp312 wheel
# 结果是每次推送 data.db 都触发部署、每次部署都失败，生产 alias 停在 2024-06-27 那次成功部署上，
# API 因此一直返回两年前的数据（HTTP 200，所以纯可达性监控毫无察觉）。
# 下面全部换成有 cp312 wheel 的版本，大版本不变，API 代码不用改。
#
# leancloud==2.9.7 依赖 gevent<22.0.0 与 Werkzeug<2.0.0，两者在 Python 3.12 上装不上；
# 本站 DATABASE=sqlite，leancloud/mongodb 后端是条件导入，不装也不影响。如要用 leancloud，
# 需要连同 Python 运行时一起降级。Werkzeug 同理：全仓库没有任何 import，它只是 leancloud 的约束。
# leancloud==2.9.7
# Werkzeug==1.0.1
fastapi==0.68.0
uvicorn==0.30.6
requests
PyMySQL==1.1.1
SQLAlchemy==1.4.54
lxml==5.3.0
pymongo==4.10.1
dnspython==2.7.0
aiohttp==3.10.11
pydantic==1.10.18
PyYAML==6.0.2
PyNaCl==1.5.0
python-jose==3.3.0
passlib==1.7.4
argon2-cffi==23.1.0
